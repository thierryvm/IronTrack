// Configuration Next.js standard sans next-pwa
const nextConfig = {
  images: {
    domains: [
      'taspdceblvmpvdjixyit.supabase.co',
      // Ajoute ici d'autres domaines si besoin
    ],
  },
};

export default nextConfig;
